"""Bootstrap pipeline — implements `livedocs bootstrap`.

Seven-phase pipeline (guidance → scan → taxonomy → review → drafts → stitch
→ refinement → global update). See .spec/bootstrap/plano-bootstrap.md for
the detailed plan.
"""
