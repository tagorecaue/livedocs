"""Prompt templates for the bootstrap pipeline.

The .md files in this package are loaded as text at runtime by the phase
modules. Keeping them as files (not Python strings) makes them easier to
iterate on and diff.
"""
